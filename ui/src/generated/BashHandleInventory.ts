// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BashHandleState } from "./BashHandleState";

/**
 * One bash handle's observability projection.
 *
 * `pid`, `pgid`, and `ring_bytes_used` exist only while the handle is live;
 * `duration_ms` exists only once it is terminal. Each is skipped on the wire
 * when absent so the TS side sees an optional field.
 */
export type BashHandleInventory = { 
/**
 * `Handle.handle_id` (e.g. `b-1`).
 */
handle_id: string, 
/**
 * Optional human-readable label set on the run call.
 */
label?: string, 
/**
 * The command, display-simplified.
 */
cmd: string, state: BashHandleState, 
/**
 * Native pid; present while live.
 */
pid?: number, 
/**
 * Process-group id; present while live.
 */
pgid?: number, 
/**
 * When the command was spawned. RFC3339 on the wire.
 */
started_at: string, 
/**
 * Wall time the handle ran for; present only when terminal.
 */
duration_ms?: number, 
/**
 * Bytes currently held in the output ring; present while live.
 */
ring_bytes_used?: number, };
