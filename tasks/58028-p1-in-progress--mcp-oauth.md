M3 of the native HTTP MCP epic (tasks/58024): OAuth 2.1 for HTTP MCP servers, per specs/mcp/ (REQ-MCP-009..013, mcp.allium OAuthPhase). Discovery (401 + WWW-Authenticate -> RFC 9728 PRM -> RFC 8414/OIDC AS metadata), client identity (preconfigured -> cached registration keyed by authorization server -> RFC 7591 DCR), auth-code + PKCE with state/iss checks and RFC 8707 resource indicators, SQLite token store with silent restore, refresh-with-rotation, refresh-failure re-prompt, and 403 insufficient_scope step-up with scope union. Native callback route + structured pending_auth_url via GET /api/mcp/status. Closes tasks/08639.
