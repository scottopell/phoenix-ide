Refine Explore bash sandbox policy after initial nono integration.

Implement broad filesystem read with explicit sensitive-path denies where enforceable, preserve inherited PATH, add synthetic sandbox HOME, macOS per-user temp compatibility, ambient credential env scrubbing, linked Git worktree status compatibility, and fix bash exit-code 128 being displayed as signal 0. Update specs without referencing external products.
