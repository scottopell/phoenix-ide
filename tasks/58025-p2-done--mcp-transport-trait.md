M1 of the native HTTP MCP epic (tasks/58024): pure transport-abstraction refactor of crates/phoenix-tools/src/mcp.rs with zero behavior change, per specs/mcp/design.md "Transport Boundary". Extract an McpTransport trait (request/notify + transport-specific health) returning the typed TransportError, with a ServerMessageSink for server-initiated messages; move the existing stdio behavior behind a StdioTransport impl (keeping the lock-stdin+stdout-for-the-round-trip serialization there only); make McpServer hold a Box<dyn McpTransport> so initialize / paginated tools-list / tools-call / notification handling are transport-agnostic. Turn McpServerConfig into the Stdio | Http sum type (HttpAuth / StaticCred / PreconfiguredClient shapes from design.md) and extend the reload reconciler PartialEq comparison to the enum. Only the Stdio variant is wired: read_all_configs keeps skipping type:http entries (M2 removes the skip). All existing stdio tests stay green; add trait-seam tests against a scripted fake transport.
