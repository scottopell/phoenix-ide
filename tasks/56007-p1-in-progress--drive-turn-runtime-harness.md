Build a separate workspace binary, drive-turn, that executes one Phoenix user turn through the production conversation runtime without HTTP or test-only injection. Reuse the real provider, database, tool registry, state machine, and continuation loop; support in-memory transient SQLite and explicit/temp file-backed SQLite; emit structured raw run results for patch-recovery A/B evaluation. Keep scope to the patch evaluation need and avoid broader harness features.
