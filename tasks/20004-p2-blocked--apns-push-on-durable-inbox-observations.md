Server-side push notifications for mobile clients: APNs (or a relay) hung on durable-workflow inbox observations, with independent durable consumers and their own receipt/cursor. This replaces the iOS best-effort background-refresh nudge tier. Blocked until durable workflow delivery is authoritative. Scope: notifier consumes inbox observations at-least-once via its own cursor, device token registration endpoint, APNs delivery, and iOS-side token registration plus notification handling replacing the local-nudge path.
