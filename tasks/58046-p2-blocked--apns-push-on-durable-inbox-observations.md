Server-side push notifications for mobile clients: APNs (or a relay) hung on the durable-workflow inbox observations (specs/durable-workflows REQ-DWF-031 — independent durable consumers with their own receipt/cursor). This is the intended replacement for the iOS stopgap nudge tier (specs/ios_client REQ-IOS-018, AttentionMonitor/BackgroundRefresh in ios/), which is best-effort BGAppRefresh polling and should be deleted when this lands. Blocked on the durable-workflows stack (PR #485 ff.) reaching authoritative wake delivery. Scope: notifier consumes inbox observations at-least-once via its own cursor, device token registration endpoint, APNs delivery, and iOS-side token registration + notification handling replacing the local-nudge path.
