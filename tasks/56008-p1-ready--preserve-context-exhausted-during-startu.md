Production startup resets all conversations to Idle before worktree reconciliation, so ContextExhausted ownership state may be erased before the reconciliation gate can preserve it. Found during PR #470 review; ordering exists on main and is unrelated to the drive-turn extraction. Verify reset_all_to_idle semantics, add startup-order coverage, and preserve stable ownership states.
