Implement client-side stale tool-result clearing per specs/stale-tool-results/ (REQ-STR-001..011).

Replace the image-only retirement window (IMAGE_HISTORY_ROUNDS, tool_msg_indices_keeping_images) in Executor::build_llm_messages_static with one unified, cache-aware retention pass governing each tool result's text and images together.

Mechanism: a persisted monotonic per-conversation clear_watermark (message sequence_id, new column, default 0, no backfill). Verdict per tool result = Cleared iff round_seq <= watermark AND producing tool is clearable, else Kept; cleared drops body+images and leaves the "[tool result cleared to save context]" placeholder with the paired ToolUse block intact. Advance the watermark only under context pressure (estimated input tokens > clear_trigger), only by whole rounds, never into the keep_recent_rounds recency floor, and only when the sweep frees >= clear_at_least tokens; advance far enough to fall under target_after_clear. Watermark only ever advances, so the cleared prefix is byte-stable between sweeps (cache reuse) and the trailing-result cache breakpoint is always Kept.

Add Tool::clearable() (default false; opt in read_file/bash/search/keyword_search/read_image/browser-reads/tmux/terminal-history/process_inspection; NOT ask_user_question/think/propose_task/subagent-submit/patch). Coordinate a cache breakpoint at/after the watermark with the existing three Anthropic breakpoints (specs/llm/anthropic.allium). Non-destructive: db::ToolContent untouched. Log results-cleared count and tokens freed. Set + benchmark default params (clear_trigger fraction, target_after_clear, keep_recent_rounds >= 2, clear_at_least_tokens).
