# Consolidate browser typing fixture

Run compatible deterministic browser typing scenarios through one panic-safe serialized browser fixture instead of separate Chrome sessions. Preserve input, textarea, React-event, clear, append, missing-selector, special-character, and password-field contracts. Capture exact launch counts, five targeted before/after CPU and aggregate isolated-test elapsed samples, deliberate fault proof, broad browser validation, and a separate PR.
