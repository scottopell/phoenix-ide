# Enforce the compatibility policy at production deployment boundaries. Reject deployment of an older Phoenix binary unless an offline operator flow has restored the database backup paired with that binary, and change release-update rollback messaging so runtime-artifact rollback cannot be mistaken for database recovery. Cover launchd, systemd, and bare Linux entry points plus the UI/status contract.
