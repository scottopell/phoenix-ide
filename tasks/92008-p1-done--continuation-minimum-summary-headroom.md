Continuation summary budget planning can produce a request with only a few hundred estimated tokens of headroom under the provider cap. Investigation in task 92007 reconstructed a prod failure where the final continuation request was estimated at ~271,793 tokens against a 272,000-token cap, leaving ~207 tokens of slack; the provider rejected it with context_length_exceeded.

Implement an explicit minimum continuation headroom floor after budget capping and user-first trimming. If retained history plus fixed continuation reserve leaves less than the floor, keep dropping oldest messages until the floor is satisfied. Add tests that model a many-message tool-heavy continuation request and assert the planner leaves meaningful slack. Do not add adaptive retry as part of this task unless new evidence shows headroom alone is insufficient.
