Global Coordinator bash target design. Post-vacation only; do not modify active Creation/Close/History/legacy-removal PRs. Public target is exactly product_conversation { product_conversation_id } or privileged host_directory { cwd }; use typed Coordinator-only full-shell policy and resolved internal target, shared BashTool engine, WorkScope admission attribution for PC targets, Coordinator attribution for host escape hatch, no fake WorkScope or confinement claim. Update global-recall/permissions/ProductConversation specs and add a superseding ADR when implemented.
