read_conversation pages still render one whole message before windowing: a single multi-MB tool-result message is materialized in full on each read_page call even though only READ_PAGE_CHARS are returned. read_page already streams across messages (stops once the page is filled), but a single oversized message is not char-windowed internally. Add per-message incremental windowing (skip/take within one rendered message) so work — not just returned text — is bounded for pathological multi-MB messages in a chain member. Surfaced during Codex review of #246 (conversation-retrieval).
