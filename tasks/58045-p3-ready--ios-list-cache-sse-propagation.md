iOS client: propagate SSE conversation_update merges from an open ConversationSession into ConversationListStore (and its disk cache). Today the list row stays stale after e.g. task approval changes cwd/branch until a full refresh succeeds, and stays stale offline. Sketch: give ConversationSession an onConversationUpdated callback that AppModel wires to listStore.upsert; make upsert preserve presentation_mode/requires_action when the incoming record lacks them (session conversations come from SSE init, which omits list-endpoint enrichment). Raised in PR #447 review (deferred there as cosmetic-staleness).
