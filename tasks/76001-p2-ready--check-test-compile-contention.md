CI run 27316908913 showed "cargo test compile" taking 331s immediately after an identical 242s codegen compile of the same artifacts — likely cargo target-lock contention with lane_e2e's bin build, possibly sccache GHA misses. The CI lane split (separate runners for rust and e2e) sidesteps it in CI, but local full `./dev.py check --all` still runs both lanes on one target dir. Instrument run_step to separate lock-wait from build time (e.g. cargo --timings or a lock-probe pre-step) and confirm or refute the contention theory; if confirmed, consider a dedicated CARGO_TARGET_DIR for the e2e bin build like clippy already has.
