Implement client-side stale tool-result clearing per specs/stale-tool-results/ (REQ-STR-001..010).

Generalize Executor::build_llm_messages_static aged-screenshot retention to full tool-result bodies: add Tool::clearable() (default false; opt in read_file/bash/search/keyword_search/read_image/browser-reads/tmux/terminal-history/process_inspection). Anchor retention to the most recent keep_recent_rounds tool rounds; activate above a context-window high-water mark; gate each pass on clear_at_least_tokens. Replace cleared result bodies with the "[tool result cleared to save context]" placeholder, keep the paired ToolUse block, send no images. Non-destructive: db::ToolContent untouched. Preserve the trailing-result cache breakpoint (round 0 always retained). Log results-cleared count and tokens freed. Set + benchmark default params (clear_trigger fraction, keep_recent_rounds, clear_at_least_tokens).
