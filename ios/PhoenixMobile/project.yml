# XcodeGen project spec. Regenerate the .xcodeproj with:
#   brew install xcodegen && xcodegen generate
# The .xcodeproj is NOT checked in; this file is the source of truth.
name: PhoenixMobile
options:
  bundleIdPrefix: com.phoenix
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

targets:
  PhoenixMobile:
    type: application
    platform: iOS
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.phoenix.mobile
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1,2"
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        GENERATE_INFOPLIST_FILE: YES
        # Personal-tool posture: Phoenix servers live on LAN/tailnet hosts
        # with self-signed TLS (or plain http in dev). ATS exceptions are
        # required to reach them; the in-app trust toggle governs actual
        # certificate acceptance.
        INFOPLIST_KEY_NSLocalNetworkUsageDescription: "Phoenix connects to your Phoenix server on the local network."
    info:
      path: Info.plist
      properties:
        CFBundleDisplayName: Phoenix
        UILaunchScreen: {}
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        NSLocalNetworkUsageDescription: "Phoenix connects to your Phoenix server on the local network."
        # Stopgap nudge tier (AttentionMonitor): opportunistic background
        # list fetches for local notifications. Replaced by APNs when
        # server-side notification support lands.
        UIBackgroundModes:
          - fetch
        BGTaskSchedulerPermittedIdentifiers:
          - com.phoenix.mobile.refresh
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight

  PhoenixMobileTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests
    dependencies:
      - target: PhoenixMobile
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "5.9"

  PhoenixMobileUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - UITests
    dependencies:
      - target: PhoenixMobile
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "5.9"

# Shared scheme so `xcodebuild test -scheme PhoenixMobile` works in CI
# without anyone having opened Xcode first.
schemes:
  PhoenixMobile:
    build:
      targets:
        PhoenixMobile: all
        PhoenixMobileTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - PhoenixMobileTests

  # Opt-in live-server journey. Kept separate so normal CI and unit-test
  # runs never depend on a running Phoenix instance.
  PhoenixMobileUIQA:
    build:
      targets:
        PhoenixMobile: all
        PhoenixMobileUITests: [test]
    test:
      config: Debug
      targets:
        - PhoenixMobileUITests
      environmentVariables:
        PHOENIX_UI_TEST_SERVER_URL: "$(PHOENIX_UI_TEST_SERVER_URL_VALUE)"
        PHOENIX_UI_TEST_CWD: "$(PHOENIX_UI_TEST_CWD_VALUE)"
